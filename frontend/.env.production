# Loaded by `vite build` (production, incl. the GitHub Pages build). The frontend
# calls the Cloud Run backend cross-origin. No secrets here — just the public URL.
VITE_API_BASE_URL=https://hakam-176728126674.me-central1.run.app
