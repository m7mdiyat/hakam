# Loaded by `vite` (dev). The frontend on :5173 calls the local backend on :8080
# cross-origin, which exercises the real CORS path (see backend HAKAM_CORS_ORIGINS).
# Run the backend with HAKAM_LOCAL=1 on port 8080.
VITE_API_BASE_URL=http://localhost:8080
