.DS_Store
node_modules/
dist/
.env
# Vite local env overrides (.env.development / .env.production ARE tracked)
frontend/.env.local
frontend/.env.*.local

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local dev store + audio (HAKAM_LOCAL=1)
.localstore/
.localaudio/

# Service-account keys / secrets — never commit
*-key.json
service-account*.json
gcp-*.json

# Design decode scratch
_decoded_*
